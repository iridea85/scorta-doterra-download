name: Release maintenance

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Le release pubbliche di Scorta doTERRA sono già disponibili."
